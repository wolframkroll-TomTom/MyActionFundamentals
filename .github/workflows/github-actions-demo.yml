name: Wolfram's workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build - just hello world.
    runs-on: ubuntu-latest
    
    steps:
      - name: first step
        run: echo "Hello world!"
